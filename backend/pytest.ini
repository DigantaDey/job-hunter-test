[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    real_ai: bypass the deterministic AI stand-in and exercise the real gateway
    live_drill: hermetic end-to-end (real app + worker + fake provider, no network)
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.SAWarning
