# Optional: real browser autofill (Playwright).
#
#     pip install -r requirements-autofill.txt
#     python -m playwright install --with-deps chromium
#
# Without this extra the application still runs — /api/settings/runtime reports
# autofill.available=false and applications are prepared (fields mapped, form
# snapshots stored) but never submitted. Standard Docker images and run.sh
# install this extra; WITH_AUTOFILL=0 opts into a smaller browser-free install.
playwright==1.63.0
